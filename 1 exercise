// 1 exercise
function getPositiveSum(...args) {
    let sum = 0;
    for (let item of args) {
        if (item > 0) {
            sum += item;
            console.log(sum);
        }
    }
}

getPositiveSum(2, -9, 5, 11, -30, 100, -8, -11, -4, 5, -6, 8)
